compile_pdf:
  image: aergus/latex
  script:
    - apt-get update -y
    - apt-get install -y inkscape
    - pdflatex -shell-escape main.tex
    - makeglossaries main
    - makeindex main.idx
    - pdflatex main.tex
    - mv main.pdf aif.pdf
  only:
    - master
    - run
  artifacts:
    paths:
      - aif.pdf
